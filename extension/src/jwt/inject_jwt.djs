very JWT
very JWT_interval

such do_nothing
wow

such get_JWT
	JWT is localStorage dose getItem with "jwt"
	rly JWT
		rly JWT is "undefined"
		but
			plz clearInterval with JWT_interval
			very message is {}
			message["funct"] is "jwt"
			message["jwt"] is JWT
			browser.runtime dose sendMessage with message do_nothing
		wow
	wow
wow

JWT_interval is plz setInterval with get_JWT 500