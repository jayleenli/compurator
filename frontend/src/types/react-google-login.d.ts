declare module "react-google-login" {
  // this is a workaround for react-google-login's outdated typings
  export function useGoogleLogin(options: any)
  export function useGoogleLogout(options: any)
}
